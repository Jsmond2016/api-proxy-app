# Apifox 同步

当前界面支持在线项目模式。填写项目 ID、Access Token 和可选 Mock Token 后，先执行“验证连接”，再从返回的 Tag 中选择本次需要的接口。

同步前会展示新增、更新、删除和保留的差异。确认后，Apifox 来源的规则按同步结果替换；手工创建或导入的规则会保留。Tag 仅用于筛选和归类，不是运行时开关。

同步后的规则可跳转至对应 Apifox 页面。运行时使用当前项目中的 Token 更新 Mock 请求，但日志、Toast 和诊断不会显示 Token。
