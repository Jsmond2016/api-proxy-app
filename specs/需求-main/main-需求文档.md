# 微信开发者工具 Apifox Mock 代理修复 需求文档

## 开发分支

`main`

## 背景与目标

本项目目标是提供一个 macOS 桌面代理工具，让微信开发者工具的 HTTP/HTTPS 请求在不修改小程序业务代码、构建配置或请求封装的前提下，经本地代理选择性转发到 Apifox Mock。

桌面应用参考项目 `/Users/huangjing/Desktop/MyCode/github/api_proxy_tool_ext` 的 Apifox 项目连接、OpenAPI 导出、Tag 筛选、规则生成、接口启停和 Mock Token 处理方式，但网络拦截载体由浏览器扩展的 `declarativeNetRequest` 改为 Tauri + Rust 本地 HTTP/HTTPS 代理。

经过多轮实现、修复、打包和用户验收，当前 `0.1.30` 已形成“配置在线 Apifox 项目 -> 按 Tag 同步接口 -> 微信开发者工具接入本地代理 -> HTTP/HTTPS 请求匹配 -> 转发 Apifox Mock -> 查看实时请求记录”的产品闭环。本文件以最终代码行为为准；早期审查问题仍保留在下文，作为历史记录，不能再视为当前缺陷。

## 最终产品概览

- 运行载体是 macOS Tauri 桌面应用，目标客户端是微信开发者工具，不是网页代理预览。
- 应用存在活动项目时自动监听 `127.0.0.1:<port>`，启动默认全量透传；只有打开全局 Mock 开关且单个接口开关开启时才拦截并转发。
- 项目通过顶部 Tabs 管理，始终至少保留一个项目；主面板集中管理 Mock 接口和请求记录。
- Apifox 当前仅支持在线模式：项目 ID、Mock 前缀、Access Token、Mock Token、连接验证、Tag 选择、接口预览和 Replace 同步。
- Mock Token 同步到目标 URL 的 `apifoxToken` 查询参数；运行时使用当前项目 Token 覆盖旧值。
- Mock 接口支持添加、编辑、删除、移动、批量移动、批量删除、测试、仅调试当前接口、单接口开关、勾选生成真机模拟提示词。
- HTTP/HTTPS 代理、动态 CA、请求实时事件、脱敏日志和关闭应用前还原微信代理提醒均已实现。

## 代码范围（可选）

| 分类 | 内容 |
| --- | --- |
| 路由 | Tauri 单窗口桌面工作台，无 Web 路由变更 |
| 代码文件 | `src/` 前端工作台、`src-tauri/src/` 原生代理与服务、Tauri 配置、测试、README 和使用文档 |
| 关联接口 | Apifox `POST /v1/projects/{projectId}/export-openapi?locale=zh-CN`、Apifox Mock URL、Tauri commands/events、本地代理 `127.0.0.1:<port>` |
| 功能点 | 项目配置、凭据、Tag 同步与激活、规则管理、HTTP/HTTPS 代理、CA 证书、微信开发者工具接入、实时日志、迁移、测试和打包 |

## 历史审查结论（已修复问题留档）

### 总体判断

早期审查曾判断安装包主要是 UI 原型，无法作为可用 MVP 交付；该判断对应 2026-08-27 的旧版本，已由后续 R1-R45 实现逐项修复。下表保留当时的问题、证据和用户影响，用于追溯，不代表 `0.1.25` 当前状态。

### 问题与证据

| 问题 ID | 严重性 | 问题 | 代码/运行证据 | 用户影响 |
| --- | --- | --- | --- | --- |
| P1 | 阻断 | 无法创建、编辑、删除真实联调项目 | `ProjectSidebar.tsx` 的新建、偏好设置、备份按钮无事件；`state.rs` 固化演示项目 | 用户只能使用假域名和演示规则，无法配置真实小程序后端 |
| P2 | 阻断 | Apifox 在线同步协议不完整 | 当前只对任意 `source_url` 发 GET；参考项目使用项目 ID、Bearer Token、API 版本头和 POST 导出请求 | 填写真实项目 ID 无法同步，在线模式名不副实 |
| P3 | 阻断 | 缺少 Apifox Mock Token | 当前数据模型和表单没有 Mock Token；参考项目会追加 `apifoxToken` 查询参数 | 需要 Mock Token 的云端 Mock 请求失败 |
| P4 | 阻断 | Tag 不能从 Apifox 获取且不能控制实际拦截 | 当前 Tag 为逗号文本；规则页 Tag 仅过滤表格，不更新 Rust 生效规则 | 无法按迭代 Tag 快速切换 Mock 接口 |
| P5 | 阻断 | HTTPS CA 流程不可操作 | 只生成 PEM；查看说明按钮无行为；`trusted` 永远为 `false` | 微信开发者工具不信任 MITM 证书，HTTPS 请求失败 |
| P6 | 严重 | 源域名字段未参与规则匹配 | `proxy/mod.rs` 仅按 method + path 匹配 | 相同路径的其他域名可能被错误转发，存在误拦截风险 |
| P7 | 严重 | 缺少真实项目的路径前缀与端口编辑 | UI 没有项目编辑入口，代理固定使用演示 Profile 的端口 | 无法限定拦截范围或解决端口冲突 |
| P8 | 严重 | 请求日志不实时 | Rust 只修改内存快照；前端仅初始化和按钮操作后读取快照，没有 event 或轮询 | 用户无法判断请求是否进入代理、命中规则或失败 |
| P9 | 严重 | 代理状态可能与真实监听状态不一致 | 启动命令在异步代理任务真正运行前写入 `running`；异步错误不推送 UI | 界面显示运行但端口可能不可用 |
| P10 | 严重 | 并发日志关联不可靠 | Handler 只保存一个 `pending_log_id` 和开始时间 | 并发请求可能把响应状态和耗时写到错误日志 |
| P11 | 严重 | OpenAPI 路径参数生成不可匹配规则 | 同步规则统一生成 `exact`，例如 `/users/{id}` 不会匹配 `/users/123` | 动态路径接口无法 Mock |
| P12 | 严重 | 同步会整体覆盖规则 | `sync_openapi` 直接替换 `profile.rules`，ID 由 method + 数组序号生成 | 手工规则、启停状态和稳定标识丢失 |
| P13 | 安全 | CA 私钥文件权限为 `0644` | 本机 `apifox-proxy-ca-key.pem` 实测为所有用户可读 | 本地 CA 私钥可能被其他用户读取 |
| P14 | 安全 | Token 没有 Keychain 持久化 | 当前仅临时传递 Access Token；无 Mock Token；无 `keyring` | 每次重复输入，且后续若直接写 JSON 会泄露凭据 |
| P15 | 一般 | 多个可见按钮没有功能 | 添加规则、批量操作、详情、清空日志、证书说明等无事件 | UI 暗示功能可用但点击无效果 |
| P16 | 一般 | 默认状态包含演示日志和假数据 | 本机落盘仍有 2 个演示 Profile 和 3 条演示日志 | 用户无法区分真实流量与样例数据 |
| P17 | 交付 | 测试无法证明核心工作流 | 现有 5 个单元测试不覆盖真实 HTTP/HTTPS、在线 Apifox、微信开发者工具和事件日志 | 编译及单测通过仍可能完全不可用 |
| P18 | 交付 | 未完成端到端验收就提前打包 | 旧方案文档明确记录 HTTP/HTTPS、CA 信任、微信接入和在线同步尚未验证 | 安装包不具备可交付质量 |

### 参考项目能力映射

| 参考能力 | 参考实现 | 桌面版要求 |
| --- | --- | --- |
| Apifox 在线导出 | 项目 ID + Access Token，POST 导出 OpenAPI | 由 Rust 发起同协议请求，返回文档摘要和可选 Tag |
| Mock Token | Mock URL 追加 `apifoxToken` | Token 存本地 Profile；同步规则目标显式携带后缀，转发时当前值覆盖旧值，日志脱敏 |
| Tag 同步 | 选择可用 Tag 后解析接口 | Tag 仅用于同步筛选和规则归类 |
| 规则启停 | 全局开关 + 单接口开关 | 代理运行状态 + 全局 Mock + 单规则启用共同决定生效 |
| 合并/覆盖 | 只替换 Apifox 来源接口，保留自定义接口 | 使用稳定来源 ID，提供合并/覆盖预览并保留用户状态 |
| 请求重定向 | 浏览器声明式规则 | Rust 代理按 host/path/method 匹配并改写至 Apifox |

## 目标用户工作流

1. 首次启动应用展示至少一个真实项目 Tab；用户填写项目名称、真实源域名、可选路径前缀和本地代理端口。
2. 用户打开“连接并同步 Apifox 接口”弹框，填写在线项目 ID、Mock 前缀、Access Token、Mock Token 并验证连接。
3. 验证成功后选择 Tag，预览接口并确认同步；同步固定 Replace Apifox 来源接口，保留 Custom/Imported 接口。
4. 用户按需编辑、删除或批量删除 Mock 接口；每条接口可独立启停、测试或进入“仅调试当前接口”。
5. 应用自动监听活动项目端口并默认全量透传。用户在微信开发者工具中将 HTTP/HTTPS 代理设置为 `127.0.0.1:<port>`；HTTPS 场景按证书弹框指引完成 CA 信任。
6. 打开全局 Mock 开关后，符合项目域名、路径前缀、Method 和接口开关的请求转发到 Apifox，其余请求原样透传。
7. 工作台实时显示请求来源、匹配决策、目标、响应码、耗时和脱敏失败原因；请求记录默认筛选已 Mock。
8. 真机调试时勾选需要的接口，点击“真机模拟”复制 AI 提示词；提示词只要求替换 URL，并带有删除 TODO 和禁止提交约束。
9. 关闭应用前确认已将微信开发者工具代理还原为直连，避免关闭本工具后客户端继续访问失效端口。

## 需求范围

| ID | 需求点 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| R1 | 真实联调项目管理 | 可创建、编辑、删除和切换项目；配置名称、源域名、路径前缀和端口；重启后保留；无演示项目污染；删除有确认 | 已确认 |
| R2 | Apifox 在线连接 | 在线模式按项目 ID + Access Token 调用 Apifox 导出 API；验证连接、校验 OpenAPI、选择 Tag、预览并确认同步；当前产品不提供 Local 模式 | 已确认 |
| R3 | 凭据与 Mock Token | Access Token、Mock Token 仅存 macOS Keychain；JSON、日志、错误和导出不含明文；Mock 请求正确追加或保留 `apifoxToken` | 已废弃：由 R15 替代 |
| R4 | Tag 发现、同步与刷新 | 从 OpenAPI 获取可用 Tag，支持搜索多选、历史组合和同步预览；刷新只更新 Apifox 来源规则并保留自定义规则 | 已确认：同步策略部分由 R18 替代 |
| R5 | Tag 激活切换 | 区分已同步 Tag 与当前激活 Tag；支持一键单选、复选、全开和全关；切换后 Rust 立即只让激活 Tag 的启用规则生效，UI 明确展示生效数量 | 已废弃：由 R13 替代 |
| R6 | 规则匹配与管理 | 按规范化 host、可选路径前缀、method 和 exact/contains/regex/template 匹配；支持规则增改删、批量启停和详情；OpenAPI `{param}` 自动转换为安全模板匹配 | 已确认 |
| R7 | HTTP 代理生命周期与透传 | 仅监听 loopback；绑定成功后才显示 running；端口冲突和运行错误实时可见；命中请求保留 method/body/必要 headers/query，未命中请求原样透传 | 已确认 |
| R8 | HTTPS CA 与 CONNECT | 支持 HTTPS CONNECT 和动态证书；CA 可生成、定位、打开和检测信任；私钥权限为 `0600`；指纹按证书 DER 计算；未信任时阻止误导性启动或明确告警 | 已确认 |
| R9 | 微信开发者工具接入 | 应用内提供当前代理地址、CA 状态、接入检查和故障诊断；真实微信开发者工具 HTTP/HTTPS 请求可进入代理，无需修改小程序代码 | 已确认 |
| R10 | 实时请求日志 | 通过 Tauri event 实时推送；每请求独立关联 ID；支持状态/Tag/规则筛选、详情、清空和最多条数限制；敏感头、Token、Body 默认不落盘 | 已确认 |
| R11 | 状态持久化与安全迁移 | 使用带 `schemaVersion` 的配置；从现有演示状态安全迁移为空状态或用户真实数据；运行时状态不伪持久化；异常写入可恢复；兼容导入参考项目 JSON 子集 | 已确认 |
| R12 | 自动化验证与可安装交付 | 单元、集成和本地 HTTP/HTTPS 端到端测试通过；完成微信开发者工具人工验收清单；构建 arm64 `.app/.dmg`，验证签名、DMG 完整性和安装启动 | 已确认 |
| R13 | 简化运行时 Mock 控制 | 移除“运行时 Mock 范围”整行、Tag 勾选和“全部 Tag 生效/暂停”按钮；所有已同步 Tag 默认参与规则匹配，用户仅通过每条接口的 Mock 开关控制是否拦截；替代 R5 | 已确认 |
| R14 | 对齐参考工具的全局 Mock 开关 | 将“全部接口 Mock/全部接口透传”两个按钮替换为一个全局 Switch；关闭时所有请求透传，开启时再按逐接口开关匹配；切换全局状态不得改写逐接口状态；现有项目升级时默认保持开启，新建项目默认关闭 | 已确认 |
| R15 | 简化 Token 本地存储 | 不再使用 macOS Keychain；Access Token、Mock Token 作为项目配置字符串写入应用数据 JSON，重启后在表单中保留并回显；仍不得写入请求日志、诊断、Toast 或错误；替代 R3 的 Keychain/JSON 禁止明文要求 | 已确认 |
| R16 | 同步规则显式携带 Mock Token | Apifox 同步规则的目标 URL 必须显式包含 URL 编码后的 `apifoxToken=<Mock Token>`；Token 更新并同步时更新所有 Apifox 来源规则，Custom/Imported 规则不改；运行时当前配置 Token 覆盖目标或原请求中的旧同名参数；请求日志仍脱敏 | 已确认 |
| R17 | 请求记录固定滚动区域 | 有数据时请求记录列表固定为 `360px` 高度并内部滚动；无数据时仅展示紧凑空状态；标题、搜索、状态筛选和清空按钮保持可见 | 已确认 |
| R18 | 简化 Tag 同步并优化规则列表 | 移除同步策略选择，Tag 同步固定替换全部 Apifox 来源规则并保留 Custom/Imported 规则；Mock 接口表格使用 Ant Design Table 自适应高度，无数据展示空状态；接口信息和 Mock 目标列设置稳定列宽并允许长内容换行；Apifox 请求路径可打开对应 Web 接口页 | 已确认 |
| R19 | 完善规则删除、重置与标题布局 | 单条规则的删除图标必须完整可见并可删除任意来源规则，删除经应用内确认后立即更新运行代理和持久化配置；在“添加规则”右侧增加“重置列表”，二次确认后清空当前 Profile 全部 Apifox/Custom/Imported 规则及已同步 Tag，但保留项目、Apifox 连接、Token、Mock 前缀和全局开关；搜索框移动到“Mock 规则”标题右侧，操作按钮保持右对齐；删除标题上方 `ROUTING RULES` | 已确认 |
| R20 | UI 基础组件迁移到 Ant Design | 使用 Ant Design 6 替换手写 Button、Input、Select、Switch、Table、Form、Modal、确认弹窗、Tooltip、反馈提示等基础组件；保持现有绿色桌面工具视觉、信息密度、功能层级、固定滚动高度和业务交互；允许使用 `classnames` 等样式辅助库，TailwindCSS 可选且不作为本轮必需依赖 | 已确认 |
| R21 | 弱化低频配置并突出 Mock 接口 | 删除顶部重复代理地址块；Apifox 连接与 HTTPS 证书收拢为按钮和弹框，主工作台突出 Mock 接口；接入检查保留唯一代理地址与复制入口；连接弹框关闭后再次打开回显当前项目配置 | 已确认 |
| R22 | 对齐参考项目的 Mock 接口命名与 URL 自动映射 | 将“Mock 规则”“添加规则”“编辑规则”“保存规则”“重置规则列表”等面向用户的规则称谓统一改为“Mock 接口”“添加接口”“编辑接口”“保存接口”“重置接口列表”；新增接口表单所有字段初始为空，接口 URL 为第一项；用户输入 URL 并失焦后，基于当前 Profile 已验证的 OpenAPI 数据按规范化路径和请求方式进行精确优先、唯一模糊匹配，唯一命中时自动回填接口名称、完整请求路径、Method、匹配模式、Mock 目标和 Apifox Web 链接所需信息，多匹配或未匹配时给出明确提示且不覆盖用户输入；编辑已有接口时继续回显已有值 | 已确认 |
| R23 | 优化连接配置与接入区布局并展示应用版本 | “连接并同步 Apifox 接口”弹框中的连接来源、Mock 前缀、Access Token、Mock Token 和验证操作自上而下排列，每个表单项独占一行，不再横向挤压；左侧菜单品牌标题 `APIFOX PROXY` 右侧以低权重文本展示当前软件版本号，版本必须来自应用构建版本而非组件硬编码；Apifox 配置与 HTTPS 证书两个入口按钮移动到“接入检查”提示条下方的独立一行，不再与提示条并排，按钮功能和状态摘要保持不变 | 已确认 |
| R24 | 代理端口常驻监听并以安全透传启动 | 存在活动项目时，桌面应用打开后必须自动监听该项目的 `127.0.0.1:<port>`，用户无需手动启动，微信开发者工具已配置该端口时不得因应用未点击开关而连接失败；每次应用启动均将所有项目的全局 Mock 状态重置为关闭，默认全量透传；Mock 接口区现有“全局 Mock”Switch 作为唯一 Mock 业务开关，关闭时所有请求透传，开启后才按活动项目域名、路径范围和逐接口开关执行 Mock；删除顶部“启动代理/停止代理”操作，仅展示监听、透传或 Mock 状态；创建首个项目后自动监听，切换项目、修改活动项目端口或删除活动项目时自动切换监听目标；无项目或端口冲突时展示明确错误，不伪装为可用；HTTPS Mock 继续遵循 CA 信任要求 | 已确认 |
| R25 | Apifox 在线模式弹框对齐参考项目并修复滚轮穿透 | “连接并同步 Apifox 接口”仅保留在线项目模式，不再展示本地 URL 模式；弹框交互参考 `/Users/huangjing/Desktop/MyCode/github/api_proxy_tool_ext` 的配置顺序、分组层级、验证连接、Tag 选择、接口预览和确认同步流程，保持现有字段回显、Token、Mock Token 和同步能力；弹框内容超出可视高度时只允许弹框内部滚动，滚轮事件不得传递给外层工作台，弹框打开期间主页面滚动位置和布局保持稳定 | 已确认 |
| R26 | Mock 接口单接口测试与仅调试当前接口 | 在每条 Mock 接口操作区增加“测试”入口，参考 `api_proxy_tool_ext` 发送当前 Mock 请求并在弹框展示请求地址、状态、响应和错误；当全局 Mock 关闭或其他接口已开启时，提供“仅调试当前接口”动作，开启全局 Mock 并关闭其他接口的逐条开关，仅保留当前接口用于调试；测试前校验当前接口开关和 Mock 目标，所有结果提供 Toast/明确状态反馈 | 已确认 |
| R27 | Mock 接口不生效排查提示 | 在“Mock 接口”标题右侧增加小型问号 Tooltip，简要提示常见不生效原因：全局/接口开关关闭、真实域名或路径前缀不匹配、Method 定义错误（如 GET 请求定义为 POST）、接口路径不匹配，以及 HTTPS 证书未信任；不改变代理行为 | 已确认 |
| R28 | Mock 测试受全局开关约束并完善结果弹框 | 全局 Mock 关闭时，Mock 接口“测试”按钮不得发起请求；测试响应体优先按 JSON 两空格格式化，非 JSON 保留原文；测试弹框展示请求/响应信息和错误信息，底部提供“重试”和右侧“去 Mock 接口”，弹框右上角关闭按钮负责关闭；“重试”在弹框内重新发起当前 Mock 请求并刷新结果 | 已确认 |
| R29 | 单主面板与 Tabs 项目管理交互 | 参考 `api_proxy_tool_ext` 移除左侧项目菜单，改为单主面板布局；多个联调项目通过顶部 Tabs 切换并管理，每个 Tab 保留项目名称和规则数量，提供新增、编辑、删除项目入口；切换 Tab 继续联动当前代理监听和 Mock 接口数据；不改变项目配置、规则、代理和 Apifox 同步契约 | 已确认 |
| R30 | 测试弹框跳转 Apifox 接口设置页 | “去 Mock 接口”使用规则 `apifoxWebUrl` 打开 Apifox 接口设置页，不打开 Mock 请求目标；无可靠链接时禁用并提示 | 已确认 |
| R31 | 测试弹框补充原始接口复制与按钮顺序 | 测试弹框展示原始接口信息，格式为 `GET /api/xxxx`，旁边提供复制图标并复制该原始接口 URL/路径；底部“去 Mock 接口”靠右，移除重复的“关闭”按钮 | 已确认 |
| R32 | 优化 Mock 接口标题和配置入口布局 | Mock 接口标题行与下方表格增加 `16px` 间距；Apifox 接口按钮不展示数量，使用主题色主按钮，并与 HTTPS 证书按钮一起靠右排列，主按钮位于右侧操作区末端 | 已确认 |
| R33 | Mock 接口表格自适应高度 | 移除 Mock 接口表格外层固定高度和内部滚动限制，使用 Ant Design Table 自然撑开；无数据时继续展示空状态 | 已确认 |
| R34 | 请求记录默认聚焦已 Mock | 请求记录状态筛选默认选择“已 Mock”，时间统一展示为 `YYYY-MM-DD HH:mm:ss` | 已确认 |
| R35 | 真机模拟提示词生成 | Mock 接口列表增加“真机模拟”按钮；用户勾选部分接口后，生成仅替换 URL 请求地址的 AI 提示词，列出原始接口与 Mock 接口映射，并写入剪贴板供 AI IDE 使用 | 已确认 |
| R36 | 真机模拟提示词防误提交约束 | 真机模拟提示词追加 `TODO: 联调接口待删除` 注释要求，并明确禁止提交这些临时 Mock 接口代码 | 已确认 |
| R37 | 请求记录空状态紧凑展示 | 请求记录无数据时仅展示紧凑空状态，不保留大块固定高度空白；有数据时继续使用固定高度滚动区域 | 已确认 |
| R38 | 顶部项目导航与代理标题层级调整 | 代理标题和状态信息置于项目 Tabs 之前；移除“当前项目 / xxx”提示；项目 Tab 编辑相关入口与 Tabs 同组展示，不再占据代理标题区域 | 已确认 |
| R39 | 优化顶部品牌、规则提示与操作控件视觉 | APIFOX PROXY 与代理标题同排；标题使用深色主题；Tooltip 分点换行；搜索框有合理宽度；Apifox 为主题色主按钮；重置接口为危险红色 | 已确认 |
| R40 | 简化 Mock 接口表格信息列 | 合并接口信息与请求列，删除独立匹配列；接口信息自上而下展示中文名称、Method + URL，并保留 URL 外链 | 已确认 |
| R41 | 修正顶部标题背景与品牌副标题 | 标题保留背景色并移除 `WECHAT DEVTOOLS` 副标题 | 已确认 |
| R42 | 恢复顶部标题深色主题 | 标题模块背景接近 `#1C2620`，品牌、标题和状态保持高对比 | 已确认 |
| R43 | 恢复标题版本号并右对齐运行状态 | 在“微信开发者工具代理”标题文字右侧恢复显示当前应用版本号；运行状态徽标固定在标题模块最右侧，与左侧标题内容两端对齐 | 已确认 |
| R44 | 移除静态项目标题并保证至少一个项目 Tab | 移除静态“联调项目”文案；始终保留至少一个真实项目 Tab，最后一个项目禁止删除 | 已确认 |
| R45 | 关闭应用前提醒还原微信代理 | 关闭桌面窗口时确认提醒用户按“设置 -> 代理设置 -> 代理 -> 手动设置代理”还原配置；确认后可靠关闭窗口，关闭失败展示错误，取消则继续运行 | 已确认 |
| R46 | 测试响应内容搜索与滚动 | 测试弹框响应内容使用固定最大高度并内部滚动；提供搜索输入框，支持 `Ctrl+F`/`Cmd+F` 聚焦搜索，输入关键字后定位并滚动到匹配内容，不改变原始响应文本 | 已确认 |
| R47 | 测试响应搜索多匹配导航 | 响应搜索支持显示当前匹配序号与总数，并提供上一个、下一个按钮；同名字段或同前缀字段存在多处匹配时可循环切换；搜索框按 `Enter` 跳到下一个匹配、按 `Shift+Enter` 跳到上一个匹配；搜索关键词变化时重置并定位到第一个匹配 | 已确认 |
| R48 | 无项目时提供首个项目创建入口 | 本地配置为空或迁移后没有项目时，主面板仍必须展示项目 Tabs 操作栏及“新建项目”按钮，用户可直接创建并编辑第一个真实项目；空状态不再要求用户点击不存在的左侧菜单；创建成功后进入正常工作台，仍保证最后一个项目不可删除 | 已确认 |
| R49 | 复合操作统一单次反馈 | “仅调试当前接口”等包含多个内部步骤的操作，成功时只展示一条汇总成功提示，失败时只展示一条错误提示；内部步骤可写入诊断记录但不得逐条弹 Toast；不改变实际开关联动和持久化行为 | 已确认 |
| R51 | 全局 Mock 切换重建 HTTPS 连接 | 用户切换全局 Mock 开关后，应用必须重启当前代理监听并提示客户端建立新连接，避免微信开发者工具复用开关关闭期间已建立的 HTTPS 透传隧道；关闭开关后仍保持请求透传，其他规则和配置不变 | 已确认 |
| R52 | HTTPS 隧道常驻 MITM 以支持动态 Mock | 代理对活动项目的 HTTPS `CONNECT`/TLS 始终建立 MITM 隧道，具体请求是否 Mock 仍由全局开关、接口开关和匹配规则决定；全局关闭时解密后原样透传，开启后立即可对复用连接中的后续请求执行 Mock；HTTPS 继续要求 CA 已信任 | 已确认 |
| R53 | 修复 Apifox Tag 下拉宽度与操作 loading 隔离，并增加规则接口信息复制入口 | 长 Tag（如“训练卡和合约月付-0826”）选择后下拉框不闪烁且宽度稳定；验证连接、确认 Tag 拉取、确认同步三个操作分别显示独立 loading；接口信息列提供复制图标，复制 `{url 中文名称}`、`{GET | POST} {/api/url}` 和 `apifox 地址：{apifox url}` 三行内容 | 已确认 |
| R54 | Mock 接口跨 Tab 单条与批量移动 | 当存在其他 Tab 时，每条 Mock 接口操作列提供移动入口，点击后选择目的 Tab 并确认移动；“真机模拟”左侧提供“批量移动”，勾选多个接口后可一次选择目的 Tab 并移动；没有其他 Tab 时两个入口均不可用并提示先创建其他 Tab；移动整批原子完成，目标存在同 ID 接口时整批拒绝，接口引用的本地 Mock 响应随接口复制到目标 Tab | 已确认 |
| R55 | 新建 Tab 预填首个 Tab 的 Apifox 配置 | Apifox 接口来源作为跨 Tab 共用的默认配置；创建第一个 Tab 之后再新建 Tab 时，新 Tab 的 Apifox 弹框预填当前首个 Tab 的项目 ID、Mock 前缀、Access Token 和 Mock Token；Tag 仍由各 Tab 独立选择，不预填首个 Tab 的已同步 Tag，也不复制规则、全局 Mock 开关或本地响应 | 已确认 |
| R56 | Mock 接口按 Tab 隔离 | 不同 Tab 可分别添加或同步同一个 Apifox 接口；Apifox 规则 ID 必须在所属 Profile 内稳定、在不同 Profile 间隔离。接口启停、编辑、删除、同步替换和代理匹配均以当前 Profile 为边界；仅目标 Tab 自身已存在同一 Apifox `operationId` 时拒绝再次移动，其他 Tab 的同名接口不得阻止当前 Tab 添加或同步 | 已确认 |
| R57 | Tab 切换静默成功 | 用户切换项目 Tab 成功时不显示“切换项目：操作成功”Toast；切换失败仍显示错误提示，开始、成功或失败状态仍写入运行诊断 | 已确认 |
| R58 | Mock 接口批量删除 | 将 Mock 接口列表右上角“重置接口”改为“批量删除”；用户勾选一个或多个接口后按钮可用，点击后必须二次确认，确认后仅删除当前 Tab 已选接口，不清空其他接口、Tag 或项目配置；未勾选时按钮禁用 | 已确认 |

## 非目标

- 不修改微信小程序业务源码、环境文件、构建配置或请求封装。
- 不实现 VPN、内核扩展、透明代理或对不遵循显式/系统代理的软件强制抓包。
- 默认不自动修改 macOS 全局系统代理；用户在微信开发者工具中显式配置代理。未来若增加系统代理开关，必须单独确认权限、恢复和异常退出策略。
- 不绕过 certificate pinning。若目标客户端启用证书固定，应用只提供明确诊断。
- 本轮不迁移参考项目的权限点、迭代文档、浏览器扩展通信、自定义 JSON 响应和团队云同步。
- 本地测试包可使用 ad-hoc 签名；公开分发、Developer ID 签名和 Apple 公证需独立发布凭据与流程。

## 注意事项

- 代理和 CA 涉及敏感网络能力，必须限制监听 `127.0.0.1`，不得默认监听局域网地址。
- 不得在测试、截图、日志、异常或文档中记录真实 Access Token、Mock Token、Cookie、Authorization 或用户业务数据。
- CA 私钥不得导出到配置备份，不得以宽松权限写盘；删除/重建 CA 必须明确提示会使既有信任失效。
- 域名匹配必须先于路径和规则匹配，禁止仅按路径跨域拦截。
- Query 合并必须同时保留原请求参数和 Mock Token；不得用原请求 Query 覆盖目标 URL 自带参数。
- Tag 仅用于同步筛选和规则归类，不再承担运行时批量启停职责。
- 全局 Mock 状态与逐接口 `enabled` 必须独立持久化；切换全局开关不得覆盖逐接口选择。
- R15 会使同一 macOS 用户及能读取其应用数据目录的进程看到 Token 明文；用户接受该风险并可在 Apifox 等设置端随时废弃 Token。
- 已有 Keychain 条目不自动读取、迁移或删除，避免再次触发系统授权；用户可自行在“钥匙串访问”中清理。
- 所有可见操作必须有完整实现、禁用条件或移除，不保留无行为的按钮。
- Apifox Web 跳转优先使用 OpenAPI 的 `x-run-in-apifox`；仅在项目 ID 和数字接口 ID 均可靠时生成兜底链接，不得从 Mock URL 猜测接口页。
- 当前工作树已有用户文件 `pnpm-workspace.yaml` 和本轮新增 `mise.toml`，后续实现不得擅自删除或回退。

## 变更记录

| 日期 | 变更 | 原因 | 影响的需求 |
| --- | --- | --- | --- |
| 2026-08-27 | 创建 `main` 分支绑定需求文档，记录当前安装包审查问题和完整修复范围 | 用户实测当前功能完全不可用，要求参考 `api_proxy_tool_ext` 修复并完成微信开发者工具 Apifox Mock 工作流 | R1-R12 |
| 2026-08-27 | 用户通过 `ac` 确认全部需求 | 进入分阶段实现与验证 | R1-R12 |
| 2026-08-27 | 根据安装包实测修正项目删除和 Tag 同步交互 | 原生 `window.confirm` 在 Tauri 中无可靠反馈；Tag 选择未按参考项目使用验证后多选下拉、接口预览和明确确认 | R1、R4、R6 |
| 2026-08-27 | 根据二次实测开放 Apifox 规则编辑/删除、持久化连接表单、修正 Merge 和新 Tag 激活、增加 Toast | 规则只读、表单丢失、跨 Tag 同步误删/不生效和操作无反馈阻碍真实调试 | R2-R7、R9-R10 |
| 2026-08-27 | 提议移除运行时 Mock 范围整行及 Tag 批量按钮，以接口 Mock 开关作为唯一运行时控制 | 用户认为该层级重复且不需要；等待确认后由 R13 替代 R5 | R5、R13 |
| 2026-08-27 | 用户通过 `ac` 确认 R13，R5 标记为已废弃 | 简化运行时控制，避免 Tag 与接口规则形成重复开关 | R5、R13 |
| 2026-08-27 | 提议将规则区两个批量按钮替换为独立全局 Mock Switch | 当前按钮会批量改写逐接口状态，与参考项目 `isGlobalEnabled` 语义及用户习惯不一致 | R6、R14 |
| 2026-08-27 | 用户通过 `ac` 确认 R14 | 按参考项目语义实施独立全局门控，保留逐接口开关状态 | R6、R14 |
| 2026-08-27 | 提议取消 Keychain，将两个 Token 作为普通项目配置持久化并回显 | 用户认为 Token 可随时在服务端废弃，不需要 macOS Keychain 的严格保护和授权提示 | R3、R15 |
| 2026-08-27 | 用户通过 `ac` 确认 R15，R3 标记为已废弃 | 取消系统钥匙串授权依赖，改用本地项目配置保存和回显 Token | R3、R15 |
| 2026-08-27 | 提议同步规则目标显式追加 `apifoxToken`，并在 Token 更新时覆盖所有 Apifox 来源规则 | 用户发现规则目标未展示 Token 后缀且 Mock 服务返回无权限 | R15、R16 |
| 2026-08-27 | 用户通过 `ac` 确认 R16 | 开始实现同步目标 Token 后缀与运行时覆盖兜底 | R15、R16 |
| 2026-08-27 | 提议为请求记录列表设置固定高度和内部滚动 | 避免实时记录持续撑高整个工作台 | R10、R17 |
| 2026-08-27 | 用户通过 `ac` 确认 R17 | 开始实现固定请求记录视口 | R10、R17 |
| 2026-08-27 | 提议移除同步策略并固定替换 Apifox 规则，同时为规则列表增加固定滚动区域、URL 外链和长内容换行 | 实际使用不需要 Merge；规则数量及长 URL 会持续撑高、撑宽工作台；参考项目支持从接口记录跳转 Apifox Web 接口页 | R4、R6、R18 |
| 2026-08-27 | 用户通过 `ac` 确认 R18 | 开始实施固定 Replace、规则滚动视口、URL 外链和长内容换行 | R4、R6、R18 |
| 2026-08-27 | 提议修复规则删除入口并增加全量重置和标题布局调整 | 操作列过窄导致删除图标被裁切；用户要求沿用参考项目的重置习惯并简化标题区域 | R6、R19 |
| 2026-08-27 | 用户通过 `ac` 确认 R19 | 开始实施单条删除可见性、全量规则重置及标题布局调整 | R6、R19 |
| 2026-08-27 | 用户通过 `ac` 确认 UI 全面迁移到 Ant Design，并补充允许使用 TailwindCSS、classnames 等样式库 | 统一基础组件行为与视觉质量，同时保持参考工具用户已经熟悉的操作流程 | R20 |
| 2026-08-27 | 提议将 Apifox 连接和 HTTPS 证书管理改为按钮触发弹框，并移除顶部重复代理地址 | 配置属于低频操作，长期占据首屏会压缩 Mock 规则区域；接入检查已提供同一代理地址 | R8、R9、R21 |
| 2026-08-27 | 用户通过 `ac` 确认 R21，并补充 Mock 接口命名与 URL 优先自动映射 | R21 进入实施；新增行为对照参考项目 `ApiFormDrawer` 和 `findApiInfoFromSwagger` 形成 R22，等待独立确认 | R6、R21、R22 |
| 2026-08-27 | 用户通过 `ac` 确认 R22 | 开始统一 Mock 接口命名并实现 URL 优先的 OpenAPI 自动映射 | R6、R22 |
| 2026-08-27 | 提议将 Apifox 连接表单改为纵向单列，在左侧品牌标题旁展示构建版本，并将 Apifox/HTTPS 证书入口移动到接入检查下方独立一行 | 当前横向表单和同一行配置入口过于拥挤，信息层级不清晰；版本号可帮助用户快速确认正在运行的安装包版本 | R21、R23 |
| 2026-08-27 | 用户通过 `ac` 确认 R23 | 开始实施连接弹框单列布局、侧栏动态版本号和接入配置入口分行 | R23 |
| 2026-08-27 | 完成 R23 并生成 0.1.9 安装包 | Apifox 连接表单已纵向排列，侧栏展示运行应用版本，配置入口已移到接入检查下方独立一行；功能契约保持不变 | R12、R21、R23 |
| 2026-08-27 | 用户确认 0.1.9 安装验证通过并要求提交 | 完成本轮 UI、Mock 接口交互和桌面安装包交付闭环 | R12、R20-R23 |
| 2026-08-27 | 提议将代理监听改为应用基础服务并以关闭 Mock 的安全状态启动 | 当前顶部开关同时控制端口生命周期，未启动时微信开发者工具指向的代理端口不可达；R24 将监听与 Mock 门控解耦，并修订 R7、R14、R21 的相关交互 | R7、R9、R14、R21、R24 |
| 2026-08-28 | 用户通过 `ac` 确认 R24 | 开始实施自动监听、默认透传和 Mock 门控解耦 | R7、R9、R14、R21、R24 |
| 2026-08-28 | 完成 R24 | 应用启动即监听活动项目端口并默认全量透传；顶部启停按钮移除；全局 Mock Switch 独立控制 HTTP/HTTPS 规则拦截；Profile/端口变化自动切换监听 | R7、R9、R14、R21、R24 |
| 2026-08-28 | 提议 R25：Apifox 连接弹框仅保留在线模式并参考 `api_proxy_tool_ext` 重做交互；修复弹框滚轮穿透导致主页面滚动 | 当前弹框布局和模式选择与参考项目不一致，Modal 内容滚动未隔离 | R21、R22、R23、R25 |
| 2026-08-28 | 用户通过 `ac` 确认 R25 | 同意仅支持在线 Apifox 项目并修复弹框滚轮隔离 | R25 |
| 2026-08-28 | 完成 R25 | 在线配置表单、验证/Tag/预览/同步流程按参考项目重排；移除 Local UI；弹框滚轮不再驱动主页面 | R25 |
| 2026-08-28 | 提议 R26：为 Mock 接口增加测试入口和仅调试当前接口能力 | 对齐参考项目逐接口调试习惯，降低验证 Mock 响应和临时关闭其他规则的操作成本 | R26 |
| 2026-08-28 | 用户通过 `ac` 确认 R26 | 同意增加测试结果弹框和仅调试当前接口的开关联动 | R26 |
| 2026-08-28 | 完成 R26 | Mock 接口操作区新增测试、仅调试当前接口、编辑和删除入口；测试结果展示状态/请求地址/响应；单接口调试自动开启全局 Mock、当前接口并关闭其他接口 | R26 |
| 2026-08-31 | 用户补充测试弹框重试操作 | 用户可能在测试弹框保持打开期间修改 Apifox Mock 接口，需要在“去 Mock 接口”左侧增加“重试”，重新请求当前 Mock 目标并更新响应结果 | R28 |
| 2026-08-31 | 用户通过 `ac` 确认并完成 R53 | 固定 Tag 下拉宽度、拆分独立 loading，并增加接口信息复制 | R53 |
| 2026-09-03 | 用户提出并确认 R54 | Mock 接口支持操作列单条移动和“真机模拟”左侧批量移动；目的地仅可选择其他 Tab | R54 |
| 2026-09-03 | 用户补充并确认 R55 | Apifox 配置不限制 Tab；新建 Tab 使用首个 Tab 的连接配置作为默认值，但 Tag 不预填 | R55 |
| 2026-09-03 | 用户补充并确认 R56 | 不同 Tab 的 Mock 接口相互隔离，允许分别添加或同步同一个接口；运行时仍仅由当前活动 Tab 的全局 Mock 开关控制 | R56 |
| 2026-09-03 | 用户补充并确认 R57 | 切换项目 Tab 时不需要显示操作成功提示 | R57 |
| 2026-09-03 | 用户补充并确认 R58 | Mock 接口列表右上角改为勾选后批量删除，并增加二次确认 | R58 |
