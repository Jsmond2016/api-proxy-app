# 项目预设

项目预设用于复用基础配置，不是完整项目备份。导出内容包括源站域名、路径前缀、建议端口、Apifox 项目 ID、Mock 前缀和 Tag；不会包含规则、本地响应、日志、证书、内部 ID 或运行开关。

默认导出不包含 Token。用户必须显式选择并经过风险确认，才能把凭据写入预设文件。包含凭据的文件是明文 JSON，不应通过不受控渠道共享。

导入前会校验格式、字段和端口。创建出的项目具有新 ID、空规则和关闭的全局 Mock；用户需要自行验证 Apifox 并同步，或手动创建规则。
