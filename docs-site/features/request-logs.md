# 请求记录与诊断

“代理流量”实时展示请求来源、目标、命中规则、阶段、状态码和耗时。可以搜索、按状态筛选、清空记录，或在已 Mock 请求上重新请求并查看当前响应。

请求记录最多保留 500 条，诊断最多保留 100 条。日志不持久化请求 Body、Authorization、Cookie 或 Token；带有 Mock Token 的 URL 会先净化再展示。

排障时优先看状态：

- 已 Mock：规则完整命中。
- 透传：请求进入代理，但未通过某个匹配阶段或全局 Mock 已关闭。
- 失败：目标改写或上游请求失败。
