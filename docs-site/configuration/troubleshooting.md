# 故障排查

## 端口未运行

检查当前项目是否存在、端口是否被其他进程占用，以及代理状态是否显示错误。修改端口后重新启动当前项目。

## CA 未受信任

确认导入的是应用当前展示路径下的证书，并在钥匙串中设置为始终信任。刷新应用中的证书状态后再测试 HTTPS。

## 请求透传

在请求记录中检查 host、路径前缀、全局 Mock、Method 和规则阶段。确保当前正在使用正确的 Tab，并确认接口开关开启。

## 同步失败

确认网络连接、项目 ID 和 Access Token。错误信息可用于定位问题，但提交反馈前应移除业务 URL、Token 与响应内容。
