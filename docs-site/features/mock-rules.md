# Mock 接口

规则按源站 host、路径前缀、全局 Mock、HTTP Method、优先级和接口开关顺序参与匹配。路径支持 `exact`、`template`、`regex` 与 `contains`，OpenAPI 的 `{param}` 模板可匹配实际动态路径。

全局 Mock 是总开关。关闭后所有请求透传，单条规则的启停状态不会被改写。使用“仅调试当前接口”会临时关闭其他已启用规则并保留当前规则。

规则可以编辑、删除、批量删除或移动到另一项目。移动多个规则时，目标项目存在冲突会使整批操作失败，不会留下部分写入。
