# 代理与证书

应用自动监听活动项目的本地端口。打开微信开发者工具代理设置后，将 HTTP 与 HTTPS 地址都填写为应用展示的 `127.0.0.1:<PORT>`。

HTTPS Mock 需要本地 CA：在应用中生成或读取 CA，使用系统应用打开证书，导入 macOS 钥匙串并设为信任，最后在应用中刷新状态。私钥由应用保存在本地数据目录且权限受限，不应复制或分享。

停止使用后，关闭微信开发者工具的手动代理设置。应用不会自动恢复这一外部设置，因此关闭窗口时会显示提醒。
