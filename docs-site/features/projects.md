# 项目与 Tabs

一个项目代表一套独立的联调范围：源站域名、路径前缀、本地端口、Apifox 配置、Mock 规则和本地响应。应用至少保留一个项目；切换项目时，只有活动项目参与运行时匹配。

新建 Tab 会继承第一个项目的基础连接字段，方便重复环境配置；规则、Tag、全局 Mock 开关和本地响应不会复制。删除、编辑、启停或同步规则只影响当前 Tab。

端口冲突会在启动时显示为错误。请修改项目端口或释放占用端口，再重新启用代理。
